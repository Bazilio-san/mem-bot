Сделать модуль и эндпоинт, который выдает страницу - песочницу, на которой есть
- выбор юзера
- чата с AI ботом (слева) 
- основное пространство, где можно увидеть всю память, для выбранного юзера, распределенную по категориям.

При вводе фразы пользователя можно нажать некоторую кнопку, которая не отправляет сообщение в бота, 
а только используется для отработки той части пайплайна, который фильтрует память.
И тогда те части памяти, которые попадут в контекст для введенной фразы, подсвечиваются.

Эта страница служит для наглядного понимания работы памяти.

Также там должна быть закладка, которая показывает события проактивности, которые ожидают выполнения, 
разделенные по категориям. 


---
Добавить слой глобальной паамяти
---

Также там должна быть закладка, которая показывает заметки пользователя.


---
Надо сделать набор фактов о боте.
Что он умеет.

---
Чувствительные данные — только с подтверждением	src/pipeline/secure.js (шифрование AES-256-GCM, согласие) - должно быть отключаемым.
--- 
продуман ли инструментарий планировшика
---



11.6. Планировщик выборки памяти 🔜 (опционально)
В архитектуре предусмотрен отдельный этап-планировщик выборки, который решал бы моделью, какие области и сколько фактов
доставать. В реализации это заменено детерминированными правилами и лимитами LIMITS в retrieve.js, что быстрее и дешевле.
Модельный планировщик остаётся опцией для сложных доменов.


Все запросы к моделям идут не на api.openai.com, а **через корпоративный LiteLLM-прокси (OpenAI-совместимый API)**; 


13. Выбор моделей по этапам + GROQ

15. Требования к логированию + цены моделей