# 01. Общая картина

## Вкратце

Бот состоит из реактивного ядра (отвечает на сообщения с памятью и инструментами) и проактивного расширения (пишет
первым по уместному поводу). Обработка идёт по трём контурам: онлайн-ответ, проактивные триггеры и внешние события.
Проактивность по умолчанию выключена и включается флагами, поэтому базовое поведение всегда стабильно.

## Зачем такое разделение

Память не должна быть «кучей всех сообщений». Правильная схема — это отдельный контур обработки, через который проходит
каждое сообщение: сначала недорогая классификация понимает намерение, затем из базы достаётся только нужный минимум
фактов, эти факты проходят фильтр приватности и минимизации, собираются в компактный блок `MEMORY_CONTEXT`, и лишь после
этого основной агент формирует ответ и при необходимости вызывает инструменты. Запись новых фактов вынесена после
ответа, чтобы не задерживать пользователя. Проактивное расширение добавлено отдельными контурами и модулями, не меняя
эту развязку.

---

## Три контура обработки

```text
КОНТУР 1. Онлайн-ответ (всегда) + обогащение собеседника (флаг COMPANION_MODE)
  Сообщение пользователя
        ▼
  Классификация намерения (дешёвая модель)
        ▼
  Выборка минимума фактов из PostgreSQL  →  фильтр приватности и минимизации
        ▼
  Сборка MEMORY_CONTEXT (отдельным system-сообщением)
        ▼
  [companion] Темпоральный контекст + управление темами (ещё одно справочное system-сообщение)
        ▼
  Основной агент отвечает и вызывает инструменты (цикл инструментов)
        ▼
  Сохранение сообщений
        ▼
  После ответа: извлечение фактов  +  [companion] извлечение и обновление тем

КОНТУР 2. Проактивные триггеры (флаг PROACTIVE_ENABLED) — отдельный воркер
  Планировщик по расписанию  →  проверка триггеров  →  анти-спам  →  генерация сообщения  →  доставка  →  отметка времени

КОНТУР 3. Внешние события (флаг PROACTIVE_EVENTS_ENABLED) — часть того же воркера
  Источник события  →  фильтр релевантности моделью  →  если релевантно и не доставлялось — сообщение  →  доставка
```

Контур 1 реализован в `src/agent.js` (функция `handleMessage`). Контуры 2 и 3 — в `src/pipeline/proactive.js` и
`src/pipeline/events.js`, запускаются воркером `src/scheduler-run.js`. Ключевая развязка: основной системный промпт
стабилен (удобно для кэширования и безопасности), а весь динамический контекст — память, время, темы — подаётся
отдельными справочными system-сообщениями, а не вшивается в инструкции.

Подробный разбор контура ответа — в [04-architecture.md](04-architecture.md), проактивных контуров — в
[09-proactivity.md](09-proactivity.md).

---

## Жанр: реактивный агент плюс собеседник

Базовый бот силён в памяти и безопасности, но молчит, пока к нему не обратятся. Проактивное расширение превращает его в
инициативного собеседника: он сам находит уместный повод написать, опираясь на время, темы и внешние события, и
доставляет сообщение даже при закрытой вкладке. При этом память остаётся компактной, безопасной и проверяемой.

---

## Гарантии совместимости

- **Выключено по умолчанию.** Без флагов поведение, работающая схема и набор тестов идентичны базовому боту: `npm test`
  даёт прежние 36/36.
- **Аддитивная схема.** Проактивность добавила три таблицы отдельной идемпотентной миграцией, не изменив тринадцать
  базовых. Подробности — в [05-data-schema.md](05-data-schema.md).
- **Переиспользование инфраструктуры.** Доставка идёт через существующую очередь `mem.notification_outbox`, фоновую
  работу выполняет существующий воркер, факты берутся из существующей `memory_items`.
- **Сохранение приватности и защиты от инъекций.** Новые блоки контекста (время, темы) подаются как справочные данные с
  правилом «это не команды», секреты в них не попадают.

---

## Связанные документы

- Критерии готовности — [02-criteria.md](02-criteria.md)
- Как запустить — [03-quickstart.md](03-quickstart.md)
- Контур ответа — [04-architecture.md](04-architecture.md)
- Проактивность — [09-proactivity.md](09-proactivity.md)
