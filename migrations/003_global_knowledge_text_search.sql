-- Неточный текстовый поиск по базе знаний (вкладка «База знаний» в админке). Расширение pg_trgm даёт
-- функцию word_similarity(), по которой ищутся записи с опечатками и другими словоформами — в дополнение
-- к точному полнотекстовому совпадению по search_tsv. Отдельный триграммный индекс не создаётся: объём
-- базы знаний мал (десятки–сотни записей), последовательный проход дешевле обслуживания индекса.
CREATE EXTENSION IF NOT EXISTS pg_trgm;
