-- migrations/007_proactivity_flag.sql
-- Мастер-переключатель проактивности на уровне пользователя. Отдельная колонка нужна, чтобы отличать
-- состояние «пользователь включил контур, но все поводы выключил» от состояния «пользователь выключил контур».
-- Идемпотентно: повторный запуск безопасен. По умолчанию выключено — новый пользователь проактивности не получает.
ALTER TABLE mem.users ADD COLUMN IF NOT EXISTS proactivity_enabled boolean NOT NULL DEFAULT false;
