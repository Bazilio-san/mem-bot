Сделать модуль и эндпоинт, который выдает страницу, на которой можно увидеть всю память,
расределенную по категориям.
На этой же страничке можно ввести фразу пользователя и увидеть, какие тексты из памяти 
войдут в контекст, который уйдет в LLM вместе с этой фразой.
Это такая песочница, в которой можно воочию увидетьи понять как работате память, чо в ней хранится сейчас.
Также должен быть режим (по кнопке) обработки фразы и пополнения памяти.
В принципе, это даже можно сделать в виде чата с тем же ботом, но основное место на экране отдано показу содержимого памяти.
----
Есть ли функция поджатия истории диалога?
Последние N(=8) реплик не трогаются вообще. 
Предыдущий контекст ужимается до заданного размера SHRINKED_SIZE (какой рекомендуется?) 
после превышения порога MAX_SIZE (какой рекомендуется? очевидно SHRINKED_SIZE < MAX_SIZE )
И надо сделать так, чтобы "ближние фразы" ужимались меньше, а "дальние - больше".
Также учесть, что есть ПАМЯТЬ, чтобы история не сильно дублировала память.
Сделай предложение, как это можно сделать.
---
Чувствительные данные — только с подтверждением	src/pipeline/secure.js (шифрование AES-256-GCM, согласие) - должно быть отключаемым.
--- 
продуман ли инструментарий планировшика
---



11.6. Планировщик выборки памяти 🔜 (опционально)
В архитектуре предусмотрен отдельный этап-планировщик выборки, который решал бы моделью, какие области и сколько фактов
доставать. В реализации это заменено детерминированными правилами и лимитами LIMITS в retrieve.js, что быстрее и дешевле.
Модельный планировщик остаётся опцией для сложных доменов.


Все запросы к моделям идут не на api.openai.com, а **через корпоративный LiteLLM-прокси (OpenAI-совместимый API)**; 


13. Выбор моделей по этапам + GROQ

15. Требования к логированию + цены моделей